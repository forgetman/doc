<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="item"
            type="lib.base.model.Form" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/selector_bg_item"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="78dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <vector.widget.ImageView
                style="@style/View.Image.Form"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="19dp"
                android:imageView_source="@{item.icon}" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="19dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    style="@style/View.Text.Form.Name"
                    android:text="@{item.title}"
                    android:textColor="@color/text_2b" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@{item.subTitle}"
                    android:textColor="@color/text_75"
                    android:textSize="14dp" />
            </LinearLayout>

            <TextView
                android:id="@+id/form_tv_range"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/margin"
                android:layout_weight="0"
                android:text="@{item.range}"
                android:textColor="@color/red"
                android:textSize="14dp" />

        </LinearLayout>

        <View style="@style/View.Divider" />

    </LinearLayout>
</layout>