<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="${logo}"
        android:label="${app_name}"
        android:supportsRtl="false"
        android:theme="@style/Theme.App.Default"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning, LockedOrientationActivity"
        tools:targetApi="m">

        <activity
            android:name=".design.ui.activity.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.App.FullScreen.ShortEdges">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".design.ui.activity.NewTipActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.MainActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.WebViewActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.SetupActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.LogoutActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.city.CityActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.city.SearchCityActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.AlbumActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".design.ui.activity.SignInActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".design.ui.activity.BindPhoneActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".design.ui.activity.MsgDetailActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="lib.um.share.WXEntryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="channel_name"
            android:value="${channel}" />

        <meta-data
            android:name="host"
            android:value="${host}" />

        <meta-data
            android:name="host_h5"
            android:value="${host_h5}" />

        <meta-data
            android:name="qqappid"
            android:value="${qqappid}" />

        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />

        <receiver
            android:name=".PushReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTRATION" /> <!-- Required  用户注册SDK的intent -->
                <action android:name="cn.jpush.android.intent.MESSAGE_RECEIVED" /> <!-- Required  用户接收SDK消息的intent -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED" /> <!-- Required  用户接收SDK通知栏信息的intent -->
                <action android:name="cn.jpush.android.intent.NOTIFICATION_OPENED" /> <!-- Required  用户打开自定义通知栏的intent -->
                <!-- <action android:name="cn.jpush.android.intent.ACTION_RICHPUSH_CALLBACK"/> &lt;!&ndash;Optional 用户接受Rich Push Javascript 回调函数的intent&ndash;&gt; -->
                <!-- <action android:name="cn.jpush.android.intent.CONNECTION"/>&lt;!&ndash; 接收网络变化 连接/断开 since 1.6.3 &ndash;&gt; -->
                <category android:name="${applicationId}" />
            </intent-filter>
        </receiver>

        <service android:name=".serv.LocationServ" />
    </application>
</manifest>
