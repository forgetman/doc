<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical">

    <ScrollView
        android:id="@+id/setting_tab_recommand_sv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:scrollbars="none"
        android:visibility="gone">

        <lib.ys.view.NoCacheGroupListView
            android:id="@+id/setting_tab_recommand_lv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:childDivider="@null"
            android:divider="@null"
            android:fadingEdge="none"
            android:groupIndicator="@null"
            android:listSelector="@color/transparent"
            android:scrollbars="none"
            android:scrollingCache="false"></lib.ys.view.NoCacheGroupListView>
    </ScrollView>

    <ImageView
        android:id="@+id/setting_tab_recommand_iv_split_line"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        tools:ignore="ContentDescription" />

</LinearLayout>