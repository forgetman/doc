<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="14dp"
    android:background="@drawable/bg_stoke_corner_blue_middle"
    tools:ignore="MissingConstraints,SpUsage">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginStart="11dp"
        android:layout_marginEnd="10dp"
        android:drawablePadding="11dp"
        android:gravity="center_vertical"
        android:text="@string/installment_payment_channel"
        android:textColor="@color/text_primary"
        android:textSize="14dp"
        app:drawableStartCompat="@drawable/installment_payment_ic_card"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/tv_repayment"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="SpUsage" />

    <TextView
        android:id="@+id/tv_repayment"
        android:layout_width="115dp"
        android:layout_height="45dp"
        android:layout_marginVertical="7dp"
        android:layout_marginEnd="6dp"
        android:background="@drawable/bg_corner_blue"
        android:gravity="center"
        android:text="@string/repaying"
        android:textColor="@color/white"
        android:textSize="14dp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>